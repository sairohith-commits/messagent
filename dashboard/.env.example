# API base URL — defaults to production if not set
VITE_API_URL=https://messagent-backend-production.up.railway.app

# For local development against the local backend:
# VITE_API_URL=http://localhost:3000
